import { type Metadata } from 'next';

export const metadata: Metadata = { title: 'Running' };

function Run() {
    return (
        <div>
            <h1>Run</h1>
        </div>
    );
}

export default Run